# harness API credentials (read by scripts/harness.py). Keep chmod 600.
# 実ファイルは .harness.env として作成し、値を入れる（.gitignore で追跡対象外）。
HARNESS_BASE=https://your-harness-host.example
HARNESS_TOKEN=replace-with-your-token
